/*Instructions:Building on top of countLetter.js -Instead of having your function countLetters return just the count of each letter, the requirement has changed to return all the indices (zero-based positions) in the string where each character is found. So for each letter, we're no longer returning just one number to represent its number of occurrences, but rather (potentially) multiple numbers to represent all the places in the string that it shows up.*/var allChar = [];function addChar(x, i) { //x = current letter, i = index  allChar.push({ letter: x, count: 1, indices: [i]});}function updateChar(y, i) { //y = pos in allChar, i = index  allChar[y]["count"] += 1;  allChar[y]["indices"].push(i);}function checkAllChar(x) { //x = current letter  for (var i = 0; i < allChar.length; i++) {    if (allChar[i]["letter"] === x) {      return i;    }  }  return false}function charPos(str) {  for (var l = 0; l < str.length; l++) { //iterate through str    var charStatus = checkAllChar(str[l]);      if (typeof charStatus === "number") {        updateChar(charStatus, l);      } else {        addChar(str[l], l);      }    }  return allChar;}//test cases//console.log(charPos("lighthouse in the house"));console.log(charPos("testestest"));